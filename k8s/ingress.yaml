apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-node
spec:
  rules:
  - host: test.com
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: my-service
            port:
              number: 3000
